paths:
  /featured_products:
    post:
      operationId: getFeaturedProducts
      parameters: []
      responses:
        200:
          description: "Status 200"
          schema:
            type: object
            properties:
              type: array
              items:
                $ref: "#/definitions/Product"
