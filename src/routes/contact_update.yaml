paths:
  /contact_update:
    put:
      operationId: contact_update
      consumes: []
      parameters:
      - name: body
        in: body
        required: true
        schema:
          type: object
          properties:
            mobiles:
              type: array
              items:
                type: string
      responses:
        200:
          description: "Status 200"
          schema:
            $ref: "#/definitions/DefaultResponse"
